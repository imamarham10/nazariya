import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  ul: {
    justifyContent: 'space-around',
    marginTop: '2px',
    marginBottom: '2px',
    padding: '5px',
  },
}));
